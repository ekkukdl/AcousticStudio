# AI 에이전트 코드 수정 및 백업 가이드라인 (AI Agent Guidelines)

**모든 AI 에이전트(서브에이전트 포함)는 이 프로젝트의 코드를 수정할 때 반드시 아래의 프로세스를 준수해야 합니다.**

## 1. 작업 환경 및 필수 AI 스킬 점검 (Environment & Required Skills Check)
새로운 작업 환경(PC)에서 프로젝트를 Clone 또는 Pull 하여 AI 에이전트가 작업에 시작할때 코드 작업을 진행하기에 앞서 가장 먼저 다음 사항들을 필수로 점검해야 합니다.
- **실행 환경(Python) 점검**: 프로젝트 요구사항에 맞는 파이썬 버전이 설치되어 있는지 점검하고, 필요한 라이브러리(`requirements.txt`)가 모두 설치되어 있는지 확인합니다. 누락된 라이브러리가 있다면 본격적인 작업 전에 미리 다운로드 및 설치를 완료하십시오.
- **필수 AI 스킬 확인**: 로컬 환경에 **`karpathy-guidelines`** 스킬이 다운로드되어 있는지 확인해야 합니다. 만약 스킬이 존재하지 않을 경우 스킬을 검색하여 다운로드(설치)를 완료하십시오.
- 위 환경 점검 및 스킬 설치가 모두 완료된 후에만 아래 가이드라인의 원칙에 따라 프로젝트 코드 작업을 진행합니다.

## 2. 사전 아이디어 추론 및 사용자와 의도 정립 (Pre-edit Idea Alignment)
어떤 기능을 새로 추가하거나 기존 코드를 수정하기 전, AI 에이전트는 **반드시 사용자에게 어떻게 구현할 것인지 아이디어를 먼저 제시하고 추론**해야 합니다. 사용자가 구상하는 방향과 AI가 제안하는 기술적 해결책, 접근법이 일치하는지 명확히 정립하고 사용자의 동의(Confirm)를 받은 후에만 실제 코드 수정을 진행하십시오. 임의로 판단하여 코드를 짜거나 대규모 수정을 즉각 강행하는 것을 엄격히 금지합니다.

## 3. 기능 작업 전 단일 폴더 통일 백업 및 시행착오 중 임의 백업 금지 (Standardized Backup & No Manual Backups During Trial & Error)
- **새로운 작업 시작 전 통일된 백업 폴더 사용**: 새로운 기능을 추가하기 전, 기존에 잘 작동하는 코드를 안전하게 보존하기 위해 반드시 `AcousticStudio/backup` 이라는 단일 지정 폴더에 현재의 주요 소스 코드(`src/`, `main.py` 등)를 복사하여 백업합니다. 
- **Zip 압축 및 파편화된 백업 절대 금지**: 백업 시 매번 zip 파일을 생성하여 누적하거나, 에이전트 임의대로 파일명 뒤에 `_backup.py`, `_old.py` 등을 붙여 생성하지 마십시오. 항상 `backup` 폴더 내부에 원본과 동일한 구조로 **덮어쓰기(Overwrite)** 하는 방식으로만 백업을 유지해야 합니다.
- **시행착오 중 백업 금지**: 코드를 수정하고 디버깅하는 '시행착오(Trial & Error)' 과정 중에는 추가적인 백업 파일(`_backup.py` 등)을 생성하지 마십시오. 이는 작업 디렉토리를 오염시킵니다. 오직 기능 추가가 완전히 성공적으로 끝나고 사용자의 최종 승인(Confirm)이 떨어진 뒤, 다음 **새로운 기능을 시작하기 직전**에만 `backup` 폴더에 덮어쓰기하여 최신 상태로 갱신합니다.
- **버전 관리는 Git과 병행**: 로컬 폴더 백업과 별개로, 코드가 정상적으로 작동하고 사용자의 승인이 완료되었을 때는 `git commit` 및 `git push`를 통해 GitHub에도 버전을 백업(저장)하십시오.

## 4. 인코딩 및 파일 수정 주의사항 (Strict Encoding Rules for Korean Characters)
- **PowerShell(Get-Content / Set-Content) 사용 절대 금지**: 한글(UTF-8) 문자가 포함된 소스 코드를 수정할 때, PowerShell 명령어를 사용하여 파일의 내용을 읽고 쓰지 마십시오. Windows PowerShell은 기본 인코딩(CP949) 문제로 인해 UTF-8 한글 데이터를 영구적으로 파괴(Mojibake/글자 깨짐)할 수 있습니다.
- **파일 수정은 반드시 정규 표현식 도구(replace_file_content)나 명시적인 Python 스크립트로 수행**: 파일 내용을 변경할 때는 반드시 시스템에 내장된 파일 수정 도구를 사용하거나, Python 스크립트 작성 시 `open(..., encoding='utf-8')`을 명시하여 안전하게 수정하십시오.

## 5. 파이썬 코드 주입 시 들여쓰기 엄수 (Strict Indentation for Code Injection)
- **메서드 중간 삽입 지양**: 새로운 UI 코드나 메서드를 클래스 내부에 주입할 때, 기존 메서드(`__init__` 등)의 중간에 코드를 끼워 넣지 마십시오. 이는 파이썬의 들여쓰기(Indentation) 규칙을 훼손하여 하위 코드가 모조리 무시되거나 삼켜지는 치명적인 버그(UI 사라짐 등)를 유발합니다.
- **클래스 최하단에 새 메서드 추가**: 새로운 기능을 위한 메서드는 항상 클래스의 가장 마지막 줄(최하단)에 올바른 들여쓰기(예: 4칸)를 맞추어 추가해야 합니다.

## 6. GitHub 푸시(Push)에 대한 엄격한 기준 (Strict GitHub Push Criteria)
- **AI 자체 테스트 결과에 의존한 푸시 절대 금지**: AI가 스스로 디버깅이나 스크립트 실행을 통해 "성공했다"고 판단하더라도 절대 푸시해서는 안 됩니다. AI의 자체 테스트 성공은 푸시의 조건이 될 수 없습니다.
- **푸시 가능 시점**: 오직 **사용자가 직접 로컬에서 프로그램을 켜서 테스트해 보고 "이 기능은 이 정도면 됐다", "완벽하게 작동한다"라고 명시적으로 승인(Confirm)** 했을 때만 커밋(Commit) 및 푸시(Push)를 수행합니다.

## 7. 프로젝트 진행 상황 지속 업데이트 (Continuous Progress Update)
모든 에이전트는 사용자의 요청을 성공적으로 수행하여 특정 기능 구현이나 버그 수정이 완료될 때마다 반드시 **`docs/project_status.txt`** 파일을 열어서 다음 항목들을 최신 상태로 유지해야 합니다.
- **진행된 부분(완료된 작업)**: 방금 해결한 버그나 새로 추가된 기능을 명확히 기록.
- **현재 진행 상황**: 전체 프로젝트 관점에서의 현 위치 요약.
- **진행해야 할 부분(To-Do)**: 아직 구현하지 않은 남은 과제나 다음 목표를 갱신.

## 8. 작업 후 잉여 파일 정리 (Clean Up Residual Files)
특정 기능 구현이나 오류 수정을 위해 임시로 생성한 테스트 스크립트(`test_*.py`), 디버깅 파일(`debug_*.txt`), 수정 스크립트(`fix_*.py`) 등은 작업이 성공적으로 마무리된 후 **반드시 삭제하여 작업 디렉토리를 깨끗하게 유지**해야 합니다.

## 9. 폴더 내 생성 파일 관리 및 경로 제한 (Strict File Generation Scope)
새로운 스크립트, 데이터 파일, 또는 출력 파일의 생성과 관리는 반드시 `AcousticStudio` 디렉토리(및 그 하위 폴더) 내에서만 생성하고 관리해야 합니다. 바탕화면이나 외부 폴더에 파일을 생성하여 작업 환경을 지저분하게 만들지 마십시오. 특히 디버깅용 텍스트 파일은 `scratch`와 같은 별도 하위 폴더를 활용하십시오.
